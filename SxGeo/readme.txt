1. Ñêîïèðîâàòü SxGeo.php è SxGeo.dat (èëè SxGeoCity.dat èëè äðóãèå áàçû http://sypexgeo.net/ru/download/) íà ñåðâåð

2. Ïîäêëþ÷èòü ôàéë SxGeo.php â ñâîé ñêðèïò, äîáàâèâ ñòðîêó
include("SxGeo.php"); 

3. Ñîçäàòü îáúåêò SxGeo
// Ïåðâûé ïàðàìåòð - èìÿ ôàéëà ñ áàçîé (èñïîëüçóåòñÿ áèíàðíàÿ ÁÄ Sypex Geo)
// Âòîðîé ïàðàìåòð - ðåæèì ðàáîòû: 
//  SXGEO_FILE   (ðàáîòà ñ ôàéëîì áàçû, ðåæèì ïî óìîë÷àíèþ); 
//  SXGEO_BATCH  (ïàêåòíàÿ îáðàáîòêà, óâåëè÷èâàåò ñêîðîñòü ïðè îáðàáîòêå ìíîæåñòâà 
//                IP çà ðàç);
//  SXGEO_MEMORY (êýøèðîâàíèå ÁÄ â ïàìÿòè, åùå óâåëè÷èâàåò ñêîðîñòü ïàêåòíîé îáðàáîòêè, 
//                íî òðåáóåò áîëüøå ïàìÿòè, äëÿ çàãðóçêè âñåé áàçû â ïàìÿòü).

$SxGeo = new SxGeo(); // Ðåæèì ïî óìîë÷àíèþ, ôàéë áä SxGeo.dat
//$SxGeo = new SxGeo('SxGeo.dat', SXGEO_BATCH | SXGEO_MEMORY); // Ñàìûé áûñòðûé ðåæèì ïàêåòíîé îáðàáîòêè

4. Îïðåäåëÿåì ñòðàíó c ÁÄ ñîäåðæàùèìè ñòðàíû (SxGeo Country)
$country = $SxGeo->getCountry($ip); // âîçâðàùàåò äâóõçíà÷íûé ISO-êîä ñòðàíû
// $SxGeo->getCountryId($ip);       // âîçâðàùàåò íîìåð ñòðàíû

5. Îïðåäåëÿåì ãîðîä (SxGeo City, SxGeo City Max)
// $SxGeo->getCity($ip);     // âîçâðàùàåò êðàòêóþ èíôîðìàöèþ, áåç íàçâàíèÿ ðåãèîíà, ñòðàíû è âðåìåííîé çîíû
// $SxGeo->getCityFull($ip); // âîçâðàùàåò ïîëíóþ èíôîðìàöèþ î ãîðîäå, ðåãèîíå è ñòðàíå
$city = $SxGeo->get($ip);    // âûïîëíÿåò getCountry ëèáî getCity â çàâèñèìîñòè îò òèïà áàçû

6.  Åñëè íóæíî îñâîäèòü ðåññóðñû - óäàëÿåì îáúåêò
unset($SxGeo);

Òàêæå ñìîòðèòå ïðèìåð sxgeo_sample.php (äëÿ åãî ðàáîòû íå çàáóäüòå ñêîïèðîâàòü 
â òîò æå êàòàëîã SxGeoCity.dat)

1. Скопировать SxGeo.php и SxGeo.dat (или SxGeoCity.dat или другие базы http://sypexgeo.net/ru/download/) на сервер

2. Подключить файл SxGeo.php в свой скрипт, добавив строку
include("SxGeo.php"); 

3. Создать объект SxGeo
// Первый параметр - имя файла с базой (используется бинарная БД Sypex Geo)
// Второй параметр - режим работы: 
//  SXGEO_FILE   (работа с файлом базы, режим по умолчанию); 
//  SXGEO_BATCH  (пакетная обработка, увеличивает скорость при обработке множества 
//                IP за раз);
//  SXGEO_MEMORY (кэширование БД в памяти, еще увеличивает скорость пакетной обработки, 
//                но требует больше памяти, для загрузки всей базы в память).

$SxGeo = new SxGeo(); // Режим по умолчанию, файл бд SxGeo.dat
//$SxGeo = new SxGeo('SxGeo.dat', SXGEO_BATCH | SXGEO_MEMORY); // Самый быстрый режим пакетной обработки

4. Определяем страну c БД содержащими страны (SxGeo Country)
$country = $SxGeo->getCountry($ip); // возвращает двухзначный ISO-код страны
// $SxGeo->getCountryId($ip);       // возвращает номер страны

5. Определяем город (SxGeo City, SxGeo City Max)
// $SxGeo->getCity($ip);     // возвращает краткую информацию, без названия региона, страны и временной зоны
// $SxGeo->getCityFull($ip); // возвращает полную информацию о городе, регионе и стране
$city = $SxGeo->get($ip);    // выполняет getCountry либо getCity в зависимости от типа базы

6.  Если нужно осводить рессурсы - удаляем объект
unset($SxGeo);

Также смотрите пример sxgeo_sample.php (для его работы не забудьте скопировать 
в тот же каталог SxGeoCity.dat)